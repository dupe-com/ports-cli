version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
  settings:
    staticcheck:
      checks: ["all", "-ST1000"] # no package-comment-on-every-file nag

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
